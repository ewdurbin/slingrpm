#!/usr/bin/env python
import sys

from slingrpm import Slinger

if __name__ == "__main__":
  slinger = Slinger()
  exit = slinger.sling()
  if exit == 1:
    import os
    os.abort()
  sys.exit(exit)
